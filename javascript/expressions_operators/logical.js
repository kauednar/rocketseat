// Operadores lógicos (logical operators)

// - 2 valores booleanos, quando verificados, resultara em verdadeiro ou falso

let pao = true
let queijo = true

// AND &&
console.log(pao && queijo)


// OR ||
console.log(pao || queijo)

// NOT ! (negar uma variavel, troco de true para false ou contrario)
console.log(!pao)  